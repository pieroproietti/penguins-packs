# RHEL/ROCKY

```
sudo dnf install \
    gcc-c++ \
    make \
    rpm-build \
    rpmdevtools
```


## setxkbmap
```
sudo dnf install setxkbmap
```

### epel
We need to enable epel
```
sudo dnf install epel-release -y
```